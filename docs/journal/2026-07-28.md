# 2026-07-28

Stage 4 human review amended the ingest contract to close an id-existence oracle that the original 404/401 precedence allowed. `/report/<id>` now returns a uniform 401 unless the id names a passive check and the supplied token matches; unknown and non-passive ids perform the same dummy-hash comparison before rejection. A 404 now means only that the path is outside the report-endpoint shape. This intentionally supersedes the contract that survived the original Mercurius arc: the leaked-share blast radius includes neither states nor registry names.

The amended full working tree passed the third and final configured Terminus round, `849f0a1b8911`, clean with zero findings.

Stage 5 passed its configured Terminus gate on the first round: `f52e2da26a1c` returned clean with zero findings. No findings were vetoed or carried forward.

Stage 5 human review accepted the dispatcher, SMTP notifier, and shared message format unchanged, then amended Mattermost from an incoming webhook to the house bot-account client in `theharnessbody/mattermost`. Scry's adapter is deliberately posting-only: it never calls `Start` or opens a websocket, and the shared client's fixed 10-second HTTP timeout supplies the in-flight bound because `PostMessage` has no context variant.

The amended stage 5 working tree passed configured Terminus round `a2dc8f46e1fb` clean with zero findings.

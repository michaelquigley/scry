# 2026-07-28

Stage 4 human review amended the ingest contract to close an id-existence oracle that the original 404/401 precedence allowed. `/report/<id>` now returns a uniform 401 unless the id names a passive check and the supplied token matches; unknown and non-passive ids perform the same dummy-hash comparison before rejection. A 404 now means only that the path is outside the report-endpoint shape. This intentionally supersedes the contract that survived the original Mercurius arc: the leaked-share blast radius includes neither states nor registry names.

The amended full working tree passed the third and final configured Terminus round, `849f0a1b8911`, clean with zero findings.

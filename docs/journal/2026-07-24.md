# 2026-07-24

Planning session (Claude, planning-agent role). Spec walked question-by-question with Michael; ten decisions settled and recorded in `docs/future/scry-work-order.md` §Decisions. The consequential ones:

- `ziti` strategy dropped from v1 entirely — v1 has zero overlay dependencies (zrok share is external plumbing too, per decision 5). Both land with the agent follow-on.
- Active checks pass through *late* silently; notification pairing rule (recovery announced iff the trouble was announced) is normative in the spec's States and Transitions.
- Report contract: bare GET = ok; optional JSON body `{status, detail}` mirrors the agent protocol's result shape.
- Agent follow-on's honest test sharpened: core = engine + contracts; children are structured detail on the agent's result, never first-class checks.

Spec amended in place to match (status line, states/transitions, strategy count, ingest contract, dark-service scenario marked follow-on, two Deferred entries). Work order drafted, grounded on reckon (config cascade, cmd/main, ui embed) and ranger (Makefile push target, AGENTS.md shape). Module path `github.com/michaelquigley/scry` per origin remote.

Open questions deliberately left for review or later stages: dd duration parsing (stage 1), hand-rolled status API vs. house ogen pattern (proposed hand-rolled), net/smtp vs. go-mail (stage 5).

Next: Michael reads the draft; then mercurius review over spec + work order together (mercurius.yaml not yet created). Nothing committed yet.

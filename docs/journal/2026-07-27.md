# 2026-07-27

Mercurius arc complete: six rounds over 2026-07-24 → 2026-07-27, session `s_xFoxk2FKwVlr`, final verdict **`ready_to_build`** (round 6: zero concerns, two advisories, both taken). Synopsis at `.mercurius/s_xFoxk2FKwVlr/_synopsis.md`; every round's decisions recorded in its `_notes.md`.

Finding trajectory: 6 architectural → 4 semantic → 3 precision → 3 (including the arc's biggest catch) → 3 behavioral pins → 0. Total: 19 concerns + 6 advisories, every one accepted and fixed — nothing rejected, nothing deferred.

The reworks worth knowing about when implementing:

- **Passive is not a `CheckStrategy`** (round 4, C1 — fallout from round 1's tick fix). `CheckStrategy` is the active-probe contract (`tcp`, `http` only). A passive check's results *are* its ingest reports; window arithmetic is a pure `internal/model` function the engine calls on sweep ticks, deriving state only — sweeps never touch `lastSeen`/`lastResult`.
- **`Notifier` takes ctx** (round 2, C4 + Michael's call): `Notify(ctx, t) error`; the dispatcher owns the 30s per-attempt deadline and an unbounded per-notifier FIFO.
- **ogen everywhere** (round 4, A2, Michael: "always ogen. just keep things standardized") — status API generated from `internal/api/specs/scry.yml`, openapi-typescript for the dashboard client.
- **State parses whole or scry doesn't start; saves are fatal on failure; every record mutation marks dirty; ingest 204 means durably recorded.**
- **Sticky failed; thresholds `>=`; sweep only degrades; announcements paired** — the notification pairing table in the work order is normative.

Planning-agent self-audit done after convergence (stale "pre-mercurius"/"in planning" framings fixed, Scope phrasing aligned to the R4-C1 seam). Both artifacts unfurled.

Next: commit, then implementation handoff — six terminus-gated stages per the work order. Model selection for the implementation agent is Michael's call.

Stage 1 implementation complete (Codex, implementation-agent role): skeleton + configuration only; stopped before engine/runtime work. The initial gate ran while Scry had no canon rubric: ad-hoc round `650dfa4adc54` found only three uppercase ordinary words in comments; fixed, and `89764df7d8ee` returned `clean`. Michael then installed the Scry rubric. Its first configured round, `f94e8752b7f8`, was clean with one constructor-naming advisory; `DefaultConfig` became `NewConfig`, and configured confirmation round `89caeb933f41` returned clean with zero findings.

Frontend production dependencies audit clean. The four high advisories reported by the full npm audit are confined to `openapi-typescript`'s Redocly-based development-time parser chain; `npm audit fix` currently leaves them unresolved. Accepted for this stage because the generator reads the committed local spec and none of that chain ships in `ui/dist`; revisit when stage 6 activates API generation or when the upstream range advances.

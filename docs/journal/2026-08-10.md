# 2026-08-10

Design session on the state-history roadmap card; spec landed at `docs/future/state-history-spec.md` with the decisions recorded in its seam census. The load-bearing call from conversation: Michael judges the seam census's "metrics and time-series: permanent" fence language already too restrictive — every addition to scry is a judgment call — so the fence gets amended (transition ledger as the judged exception, "permanent" softened to standing judgment) rather than the design routing around it. Storage is a JSONL ledger, not sqlite, with sqlite explicitly the fallback pattern to copy from other house projects if query needs ever outgrow a window scan. History writes are fatal like state saves; retention is keep-everything; daemon start/stop events plus a new state-file `saved` stamp (periodic flush becomes unconditional) bound unwatched gaps so the strip never paints false green over an outage.

Spec approved by Michael; work order drafted at `docs/future/state-history-work-order.md`, grounded in the engine/state/server/ui code. Four contract-visible deltas from the spec are listed in its own deltas section for the mercurius round to adjudicate rather than folded in silently — `watching_at_from`, the 400/error schema, the `Repository` seam carrying the `saved` stamp, and the ledger being required (not nil-tolerated) at engine construction. Mechanism choices worth their rationale: open-per-append (statelessness across year rollover beats a cached descriptor at this volume), a store-level RWMutex (engine appends and API reads share one store instance, removing the torn-tail read race), clock threading (`at` captured once per command and passed to persist, preserving read-once-per-input when `Save` gains a timestamp).

Mercurius session `s_37Md9x4JpHSh` ran ten rounds over the spec and work order and closed `ready_to_build` (synopsis in `.mercurius/s_37Md9x4JpHSh/`). 36 findings walked one per turn: 31 fixed, 2 vetoed (fetch-generation tracking for in-flight restarts; the r5 removal-boundary patch), 3 dissolved by simplification. The arc's defining pattern was Michael's complexity calls cutting whole subsystems mid-review, each recorded in the spec's census and guarded in `mercurius.yaml`: the removal-event apparatus became ignore-and-warn plus an offline `scry prune <name>` (round 5), and the split read tier ("older segments fail requests, not boot") became one law with the state file — the whole ledger parses at boot or scry does not start (round 7).

Two things happened after the clean verdict that a later agent must know. First, a top-to-bottom coherence audit fixed ten drift spots the incremental edits had left (the worst: stage 5 still instructed the implementer to write the deleted split-tier language into `docs/current/seam-census.md`, and the server `HistoryReader` seam showed a pre-optional-bounds signature). Second, Michael ordered a third simplification round 10 never saw: the append-leads-persist ordering flipped to persist-first, deleting boot corrections, the tail walk, and `BootRecord` entirely — `Boot` now takes just the configured id set. The recorded trade: a crash in the persist-to-append window loses at most the final transition, and in the worst case (a sticky `failed`) the strip's tail disagrees with the row until recovery, with the row and pager authoritative throughout. That divergence-from-verdict is deliberate, operator-decided, and simplifying in direction; terminus gates the implementation regardless.

Live state: both artifacts uncommitted, awaiting Michael's read and commit; the uncommitted diff also carries the calibrated `mercurius.yaml`. Next per the pipeline: implementation agent (Michael selects per the character of the work), five terminus-gated stages. UI decisions (strip placement, gap treatment, 90-day window) remain deliberately loose for the eye pass.

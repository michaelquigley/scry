# 2026-08-06

Added `.github/workflows/ci.yml` mirroring mercurius: test → stamped build → draft release on `v*` tags. Scry-specific delta: both the test and build jobs build the frontend first (`npm ci` + `npm run build` in `ui/`), because `go vet`/`go test`/`go build` all compile the ui package whose `go:embed` requires `ui/dist`. The stamped-build path was verified locally under simulated Actions env: push's `version.sh` handles the tagless repo (`v0.0.0-dev.<date>.<sha>` fallback), so CI is green-able before the first `v` tag. Artifact tars README and CHANGELOG only — scry has no LICENSE file (flagged to Michael; mercurius ships one and the repo is public).

Added the `sendmail` notifier (Claude, review-companion acting as implementer at Michael's direction): a third `Notifier` implementation execing the host MTA's sendmail binary — chosen over adding SMTP auth because the MTA's store-and-forward queue beats the dispatcher's bounded retries for the failure case that matters, and no credential enters scry's config. Boot-fatal binary stat in the constructor (registry error path, like NewSMTP); `exec.CommandContext` under the attempt ctx so the deadline bound is native; `-i -f <from>` plus explicit recipients; shared message format with local line endings (the MTA owns wire form). The no-exec fence is the agent protocol's network surface — a config-named local binary behind the Notifier facade doesn't touch it; the notifications doc says so explicitly. HQ deployment should prefer sendmail over the smtp block per the host's already-configured MTA.

Two implementation catches worth keeping: the stalled-binary test exposed that `exec.CommandContext` kills the process but `Wait` blocks on output pipes held by forked children (postfix's `postdrop` pattern) — `WaitDelay = 1s` is the fix, and the test proves a stalled MTA returns the attempt in ~1.1s instead of holding it 30s. Terminus round `36bd2fc5a3e4` then caught the execute-bit check accepting binaries the daemon's own user cannot run (mode 0001-class); replaced with `exec.LookPath`, which performs a real access check for the current process, plus a rejection test for an inapplicable-class execute bit. Confirmation round `8e7ad177554b` returned clean with zero findings.

Dashboard sort tweak at Michael's request: the secondary sort within each state group is now id ascending instead of `since` descending — stable, scannable order between polls; trouble-first stays primary. One comparator line plus the doc sentence; below the substantive bar, so no terminus round (deliberate).

Dashboard timestamps now carry ages (Michael's request): `formatTimestampWithAge` renders "<local time> · <duration> ago". Two reference frames, deliberately different: last-transition ages measure against the document's `generated` stamp (the daemon's arithmetic, clock-skew-free, consistent with the in-state column), while the header's "as of" age measures against a `polledAt` state stamped on every poll attempt — including failed ones, where repeated `setStale(true)` alone would not re-render — so a stale page's age visibly grows during an outage. Below the substantive bar; no terminus round.

Dashboard restyled to the house language (Michael's request): light mode, Source Serif 4 / Source Code Pro via @fontsource-variable packages — the ranger pattern, which matches Michael's stated font pairing where reef/flo still carry JetBrains Mono. Palette is reef's cool paper and ink scale with scry's three state colors mapped to reef's success/warn/danger. Serif for language (title, check names), mono for machine strings (ids, chips, timestamps, durations, detail). Fonts bundle through vite, so the embedded UI still makes zero remote requests.

Ages now count live (Michael caught the "as of" counter pinned at 0s — generated and the poll stamp refreshed together). A one-second pulse re-renders; each age is the daemon-computed span (timestamp → `generated`) plus locally elapsed time since the document arrived (`receivedAt`, stamped only on successful polls — stamping on failures would undercount a stale document's ages). Both terms stay in their own clock frame, so skew never enters. The in-state column ticks live too.

Added `estate_name` (Michael's request): config (default `scry`, authored-blank invalid) → status contract (`estate`, required, additive) → dashboard header and browser title. Traveled through the API walk per the render seam — the page learns the name from the document, never from a build-time constant. Both generated sides regenerated from the spec; handler constructors gained the estate parameter.

Terminus round `461569ac8ae5` over the full working tree — sendmail notifier, id-stable sort, live ages, house restyle, and estate name together — returned clean with zero findings. (The sendmail-only round earlier was `36bd2fc5a3e4` → one blocking execute-access finding → fixed → `8e7ad177554b` clean.)

Favicon + footer branding per reef/flo's pattern: `ui/public/` now carries `scry-logo.svg` (wordmark rendered from reef's Source Serif 4 Variable TTF via opentype.js in the scratchpad — no repo tooling added), the shared `metawoo-logo.svg` copied verbatim from reef, and a favicon set (scrying-orb glyph — indigo disc with a paper gleam — as SVG plus PIL-supersampled ico/PNGs/apple-touch). Scry's identity color is the wordmark/accent indigo `#312e81`, now `--accent` in the palette; distinct from reef's teal and flo's green-teal. Footer mirrors reef's structure; the outbound link goes to github.com/michaelquigley/scry since scry has no metawoo product page. Visual-only; no terminus round (deliberate).

Branding redone to the actual house spec after Michael caught the wordmark font: the canonical pattern lives in the grimoire (`design/web/metawoo-software-products-identity-pack`) and the generator in `archive/assets/branding/_source/generate_marks.py` — wordmarks are Archivo Black (the metawoo publisher face), favicons are a rounded accent tile with the product initial at 62% height in metawoo paper. Reproduced the generator's exact math in node (opentype.js + wawoff2 + resvg in the scratchpad; fontTools unavailable). Scry's accent moved from my improvised indigo to sky-900 `#0C4A6E`, extending the product family metaphor — reef below (teal-900), flo the water (teal-600), scry the sky watching — flagged for Michael's veto. Also removed the check-row hairlines; the header underline stays.

Footer alignment fix (Michael caught scry riding high): the house pattern center-aligns both wordmarks at one shared height (16.25px, per reef's CSS — my differing heights and metawoo opacity fade were off-pattern, removed). That works for reef/flo/metawoo because none carry descenders — their baselines all sit at ~92.5% of their tight boxes; "scry"'s y-descender puts its baseline at 69.4%. Computed the compensation from Archivo Black metrics (3.71px down at the shared height) and applied it as a translateY on the scry logo with the derivation in a comment.

Scry wordmark bumped to 18px in the footer (metawoo stays 16.25px — product mark leads slightly); the baseline shift is height-dependent and recomputed to 3.4px, with the formula in the CSS comment so the next height change recomputes instead of guessing.

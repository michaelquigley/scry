# 2026-08-06

Added `.github/workflows/ci.yml` mirroring mercurius: test → stamped build → draft release on `v*` tags. Scry-specific delta: both the test and build jobs build the frontend first (`npm ci` + `npm run build` in `ui/`), because `go vet`/`go test`/`go build` all compile the ui package whose `go:embed` requires `ui/dist`. The stamped-build path was verified locally under simulated Actions env: push's `version.sh` handles the tagless repo (`v0.0.0-dev.<date>.<sha>` fallback), so CI is green-able before the first `v` tag. Artifact tars README and CHANGELOG only — scry has no LICENSE file (flagged to Michael; mercurius ships one and the repo is public).
